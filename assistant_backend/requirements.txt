# Core Framework
fastapi>=0.68.0
uvicorn>=0.15.0
mangum==0.17.0
pydantic>=1.8.2
pydantic[email]==2.7.0
pydantic-settings==2.6.1
python-dotenv==1.0.0
starlette==0.36.3

# Database and Storage
sqlalchemy>=1.4.23
psycopg2-binary==2.9.9  # PostgreSQL adapter
boto3==1.34.34  # AWS SDK for DynamoDB
alembic==1.13.1  # Database migrations
sqlalchemy-utils==0.41.1
azure-storage-blob==12.24.0  # Avatar uploads (prod: Azure Blob; local: Azurite, same SDK)
azure-storage-queue==12.17.0  # KEDA scaling-test job queue (prod: Storage Queue; local: Azurite, same SDK)
azure-identity==1.19.0  # DefaultAzureCredential -- Workload Identity in AKS

# Observability (traces + metrics -> shared OTel Collector -> Tempo/Prometheus,
# terraform/observability). Logs aren't shipped by this SDK -- the Collector's
# filelog receiver already tails every pod's stdout cluster-wide; the logging
# instrumentation below only injects trace/span IDs so log lines correlate
# with traces in Grafana.
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-sqlalchemy==0.65b0
opentelemetry-instrumentation-logging==0.65b0
opentelemetry-instrumentation-requests==0.65b0

# Authentication and Security
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4  # Password hashing
python-multipart>=0.0.5  # Form data parsing
bcrypt==4.1.2
cryptography==42.0.2
PyJWT==2.8.0  # JWT operations

# Middleware and Extensions
python-json-logger==2.0.7
cors==1.0.1

# Testing
pytest==8.0.0
pytest-cov==4.1.0
pytest-asyncio==0.23.5
httpx==0.26.0
moto==4.2.13
pytest-env==1.1.3
faker==22.6.0
coverage==7.4.1

# Development Tools
black==24.1.1  # Code formatting
isort==5.13.2  # Import sorting
flake8==7.0.0  # Code linting
mypy==1.8.0  # Type checking
pre-commit==3.6.0  # Git hooks

# Monitoring and Logging
sentry-sdk==1.39.1
prometheus-client==0.19.0
# OTel packages live in the "Observability" section above -- these two
# lines used to duplicate/conflict with it (opentelemetry-api==1.21.0 /
# opentelemetry-sdk==1.21.0 / opentelemetry-instrumentation-fastapi==0.42b0,
# a leftover from an earlier attempt that was never actually wired into any
# app code -- confirmed via grep, zero imports anywhere). Removed rather
# than reconciled, since nothing depended on the old versions.

# Task Queue and Background Jobs (Optional)
celery==5.3.6
redis==5.0.1

# Documentation
mkdocs==1.5.3
mkdocs-material==9.5.3
openai==1.86.0