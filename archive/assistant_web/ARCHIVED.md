# Archived — replaced by assistant_web_next

This was the original Create React App frontend. It's been replaced by
`assistant_web_next/` (Next.js 14, Pages Router, static export) and is no
longer part of the build (see `../../docker-compose.yml`).

Kept here for reference during the cutover period — diffing against it if
something in the new app looks wrong, or pulling a piece of logic that
didn't get carried over. Safe to delete once the new app has been in use
for a while without needing to consult this.
